# ========================================
# Nacos 共享配置 - Sentinel 配置（Docker 版本）
# 适用于: 所有微服务
# ========================================

spring:
  cloud:
    sentinel:
      enabled: true
      eager: true
      transport:
        dashboard: 172.20.0.17:8858
