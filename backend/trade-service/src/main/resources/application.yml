# trade-service 本地配置
# 公共配置已迁移至 Nacos: shared-common.yml

server:
  port: 9003

spring:
  application:
    name: trade-service
  cloud:
    nacos:
      discovery:
        server-addr: nacos:8848
        group: DEFAULT_GROUP
        fail-fast: false
        enabled: true
      config:
        server-addr: nacos:8848
        group: DEFAULT_GROUP
        file-extension: yml
  datasource:
    url: jdbc:mysql://mysql:3306/trade_trade?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: mysql123
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    baseline-version: 0
    validate-on-migrate: true
    encoding: UTF-8
  data:
    redis:
      host: redis
      port: 6379
      database: 13
      password: "redis123"

logging:
  level:
    com.trade.trade: DEBUG

# Spring Boot Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
  info:
    env:
      enabled: true

info:
  app:
    name: trade-service
    description: 浜ゆ槗鏈嶅姟
