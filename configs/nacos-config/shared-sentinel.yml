# ========================================
# Nacos 共享配置 - Sentinel 配置
# 适用于: 所有微服务
# ========================================

spring:
  cloud:
    sentinel:
      enabled: true
      eager: true
      transport:
        dashboard: 127.0.0.1:8858
