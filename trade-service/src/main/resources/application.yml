# trade-service 本地配置
# 公共配置已移至 Nacos: shared-common.yml, shared-sentinel.yml

server:
  port: 9003

spring:
  application:
    name: trade-service
  datasource:
    url: jdbc:mysql://localhost:3306/trade_trade?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: root123
  data:
    redis:
      database: 13
      password: "123456"
  cloud:
    alibaba:
      seata:
        tx-service-group: trade-system-group

logging:
  level:
    com.trade.trade: DEBUG
